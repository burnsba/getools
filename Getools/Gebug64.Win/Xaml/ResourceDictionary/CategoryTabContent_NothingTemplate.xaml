<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    xmlns:root="clr-namespace:Gebug64.Win"
                    xmlns:viewmodels="clr-namespace:Gebug64.Win.ViewModels"
                    xmlns:ctvm="clr-namespace:Gebug64.Win.ViewModels.CategoryTabs"
                    mc:Ignorable="d"
                    x:Name="CategoryTabContent_NothingTemplate"
                    >
    <DataTemplate
        x:Key="CategoryTabContentNothingTemplate"
        DataType="{x:Type ctvm:TabViewModelBase}"
        >
        <DataTemplate.Resources>
        </DataTemplate.Resources>
        
        <!--
        This content is being shown via a ContentPresenter/ContentTemplateSelector, so fix the datacontext
        -->
        <!--<Grid
            DataContext="{Binding
                Path=DataContext,
                RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Border}}"
                d:DataContext="{d:DesignInstance Type={x:Type ctvm:TabViewModelBase}}"
            >
            
        </Grid>-->
        <TextBlock>content: nothing</TextBlock>
    </DataTemplate>
    
</ResourceDictionary>