using System;
using System.Collections.Generic;
using System.Text;

namespace Getools.Lib.Game.Asset.Stan
{
    // flags for the tile, not language keyword flags.
    public enum StanFlags
    {
        Normal = 0,
        Kneeling = 1,
        Ladder = 3,
    }
}
